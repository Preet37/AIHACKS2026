# conjure local configuration template
# Copy to .env for local development. Keep real secrets out of Git.

# Agent provider: devin, claude, or nemotron
CONJURE_AGENT_PROVIDER=devin
CONJURE_DEMO_MODE=true

# Devin cloud agent
DEVIN_API_KEY=
DEVIN_ORG_ID=
DEVIN_API_BASE_URL=https://api.devin.ai/v3
DEVIN_MODE=normal
DEVIN_REPOS=Preet37/AIHACKS2026
DEVIN_BRANCH=feat/Devin
DEVIN_POLL_INTERVAL_SECONDS=5
DEVIN_MAX_POLL_ATTEMPTS=720

# Claude local tool-loop agent
ANTHROPIC_API_KEY=
CONJURE_ANTHROPIC_MODEL=claude-sonnet-4-6

# NVIDIA Nemotron local tool-loop agent through ChatNVIDIA
NVIDIA_API_KEY=
NVIDIA_MODEL=nvidia/nemotron-3-super-120b-a12b
NVIDIA_API_BASE_URL=

# Orkes AgentSpan agent for "find items on this page" tasks (the side-panel
# "Find on this page" button). `docker compose up -d` starts the server on :6767.
# The server makes the LLM call, so the model's provider key must be set
# (ANTHROPIC_API_KEY above for an anthropic/ model). No demo fallback.
AGENTSPAN_SERVER_URL=http://localhost:6767/api
AGENTSPAN_LLM_MODEL=anthropic/claude-sonnet-4-6
AGENTSPAN_MAX_RESULTS=6
# Encryption key for the AgentSpan server container. Generate with: openssl rand -base64 32
AGENTSPAN_MASTER_KEY=
AGENTSPAN_POSTGRES_PASSWORD=agentspan

# Redis datastore, memory, sandbox cache, and job streams
REDIS_URL=redis://localhost:6379/0
CONJURE_REDIS_URL=redis://localhost:6379/0
REDIS_NAMESPACE=conjure:dev
SANDBOX_RESULT_TTL_SECONDS=86400
CONJURE_SANDBOX_RESULT_TTL_SECONDS=86400
CONJURE_REDIS_FALLBACK=true
CONJURE_MEMORY_DEMO=true

# Browserbase sandbox infrastructure
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
BROWSERBASE_SESSION_REGION=
BROWSERBASE_REPLAY_ENABLED=true

# Simular autonomous tester / red-team driver
SIMULAR_API_KEY=
SIMULAR_BASE_URL=
SIMULAR_AGENT_MODEL=

# Sentry observability
SENTRY_DSN=
SENTRY_SANDBOX_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=0.0

# Backend service
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
BACKEND_URL=http://127.0.0.1:8000
BACKEND_WS_URL=ws://127.0.0.1:8000/ws
CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
GENERATED_CODE_ROOT=demo_code
SANDBOX_MAX_HEAL_ITERATIONS=3

# Chrome extension build-time config exposed through Vite.
# Values prefixed with VITE_ are bundled into the extension.
VITE_BACKEND_URL=http://127.0.0.1:8000
VITE_BACKEND_WS_URL=ws://127.0.0.1:8000/ws
VITE_CONJURE_WS_BASE_URL=ws://127.0.0.1:8000
VITE_CONJURE_PROJECT_ID=local-demo
VITE_EXTENSION_ENV=development
VITE_SENTRY_DSN=
VITE_ENABLE_SANDBOX_UI=true

# Optional smoke-test target. Leave blank unless a backend is already running.
CONJURE_SMOKE_BACKEND_URL=
