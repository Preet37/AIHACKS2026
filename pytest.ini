[pytest]
testpaths =
    tests
    backend/tests
python_files = test_*.py
addopts = -ra
markers =
    smoke: non-destructive repo scaffold checks
    integration: opt-in checks against running local services
